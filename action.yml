name: 'Clay Chipps'
description: 'Manage SF API Version'
author: 'Clay Chipps'
inputs:
  api-version:
    required: true
    description: 'api version in the format XX e.g 58'
runs:
  using: 'node18'
  main: 'dist/index.js'
